const calculate = {
  "/": (num1, num2) => num1 / num2,
  "+": (num1, num2) => num1 + num2,
  "-": (num1, num2) => num1 - num2,
  "*": (num1, num2) => num1 * num2,
  "=": (num1, num2) => num2,
};

export default calculate;
